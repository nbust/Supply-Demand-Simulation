package firstPackage;

public class producerCurve {
	
	private Point[] pCurve;
	
	public producerCurve() {
		pCurve = new Point[];
	}
	
	public producerCurve(int m, int n) {
		pCurve = new Point[];
	}
	
	public String toString() {
		// print out array --- olivia
	}
	
	boolean add (Point p) {
		// make new array with all old data, fill last slot with new point -- noah
	}
	
	boolean remove (Point p) {
		// use search to find one in array then remove it -- noah
	}
	
	boolean isOnCurve (Point p) {
		// scan through array and check point.equals == true -- justin
	}
	
	private int searchIndex(Point p) {
		int index = -1;
		
		for (int i=0; i < pCurve.length; i++)
			if(p.equals(pCurve[i]))
				index = i;
		return index;
	}
}
